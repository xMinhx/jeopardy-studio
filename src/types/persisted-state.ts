/**
 * Re-exports the canonical PersistedState from the Zod schema module.
 * This file exists for backwards-compatibility; prefer importing directly
 * from `@/types/schema` in new code.
 */
export type { PersistedState } from "./schema";
